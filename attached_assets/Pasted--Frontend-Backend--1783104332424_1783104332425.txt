افحص ربط الـ Frontend بالـ Backend وقاعدة البيانات كاملاً من منظور ثلاثة أطراف وأصلح كل خلل فوراً.
⚠️ قاعدة أساسية:
كل عملية يجب أن تُفحص من منظورين:

المدير — يرى كل شي ويقدر يعمل كل شي
الموظف — يرى ويعمل فقط ما يسمح له به صلاحياته
الأمان — الموظف لا يقدر يصل لبيانات خارج صلاحياته حتى لو حاول مباشرة عبر الرابط أو الـ API


1. افحص كل الـ API calls:

قارن كل API call بـ api.ts مع كل Endpoint بالـ Backend
أي API call ناقص → أضفه
أي Endpoint ناقصة → أنشئها

2. افحص كل عملية CRUD من منظور كل طرف:
العمليةالموظف (حسب صلاحياته)المديرDBإضافة مريض✅/❌ إذا عنده can_open_patient✅/❌✅/❌تعديل مريض✅/❌ إذا عنده can_edit_patient✅/❌✅/❌حذف مريض✅/❌ إذا عنده can_delete_patient✅/❌✅/❌إضافة جلسة✅/❌ إذا عنده صلاحية القسم✅/❌✅/❌تسجيل دفعة✅/❌ حسب صلاحيته✅/❌✅/❌إضافة مصروف✅/❌✅/❌✅/❌طلب شراء✅/❌ إذا عنده can_purchase_reqs✅/❌✅/❌طلب سلفة✅/❌✅/❌✅/❌صرف راتب❌ ممنوع على الموظف✅/❌✅/❌إيداع بالصندوق✅/❌ إذا عنده can_drawer_deposit✅/❌✅/❌سحب من الصندوق✅/❌ إذا عنده can_drawer_withdraw✅/❌✅/❌إنشاء سند✅/❌ إذا عنده can_vouchers✅/❌✅/❌تسوية دين✅/❌ حسب صلاحيته✅/❌✅/❌إضافة موظف❌ ممنوع على الموظف✅/❌✅/❌تعديل صلاحيات❌ ممنوع على الموظف✅/❌✅/❌إنشاء تعميم❌ ممنوع على الموظف✅/❌✅/❌عرض تقارير مالية كاملة❌ ممنوع على الموظف✅/❌✅/❌نسخ احتياطي❌ ممنوع على الموظف✅/❌✅/❌
3. افحص عزل البيانات بين الموظفين:

الموظف يشوف مرضى قسمه فقط ✅
الموظف يشوف مصروفاته فقط ✅
الموظف يشوف صندوق قسمه فقط ✅
الموظف يشوف سلفه فقط ✅
الموظف يشوف راتبه فقط ✅
الموظف لا يشوف بيانات موظف آخر ✅

4. افحص أمان الـ API:

كل Endpoint يتحقق من هوية المستخدم (Authentication) ✅
كل Endpoint يتحقق من صلاحية المستخدم (Authorization) ✅
موظف لا يقدر يستدعي Endpoint خاصة بالمدير ✅
موظف لا يقدر يصل لبيانات موظف آخر عبر API ✅

5. افحص كل عملية جلب بيانات (GET):

كل شاشة تجلب بياناتها من DB عبر API ✅
الموظف يجلب بياناته حسب staff_id وقسمه فقط ✅
المدير يجلب كل البيانات بدون فلتر ✅
إذا DB فاضية → يظهر Empty State واضح ✅

6. افحص الـ Real-time:

الـ Polling كل ثانية يعمل صح ✅
تحديث بيانات موظف لا يؤثر على بيانات موظف آخر ✅
إشعارات تصل للطرف الصحيح فقط ✅

7. افحص قاعدة البيانات:

كل جدول مستخدم بالكود موجود بـ DB ✅
كل عمود مستخدم بالكود موجود بالجدول ✅
العلاقات (Foreign Keys) صحيحة ✅

⚠️ تعليمات صارمة:

افحص كل شي بالتوازي
أصلح كل خلل فوراً بدون انتظار
لا تغيّر أي تصميم أو واجهة
لا تحذف أي كود موجود
أعطني تقرير منفصل لكل طرف (موظف/مدير/DB/أمان) بـ ✅/❌