
  # Build

  This is a code bundle for Build. The original project is available at https://www.figma.com/design/vKajgzCSGxxRNOuRPPOsPY/Build.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  