- [Patient screen architecture](patient-screens.md) — three-screen patient flow: OpenPatient (split search), NewSession (existing patient), PatientFile (history tabs); debt sourced live from debts state.
- [App data model](app-data-model.md) — all state in App root; doDeposit/doWithdraw wire drawer balances; sessions+debts are two separate lists that must stay consistent when settling.
- [DB FK quirks](db-fk-quirks.md) — ALL dept FKs to departments must be dropped (15 tables); departments table holds only custom depts; built-in IDs are frontend constants not seeded; invoices.remaining is GENERATED ALWAYS; sessions_patient_id_fkey must be dropped before seeding.
- [Staff salary form](staff-salary-form.md) — percentage/dept fields must render for salary types: percentage, both, AND daily; daily shows dept revenue info box; percentage/both show per-employee session stats.
- [Radiology uploads](rad-uploads.md) — uploadedResults is Record<number,string[]> (multiple files per session); file input has multiple; handleRemoveFile removes individual files; board always shows upload button.
- [DB SSL config](db-ssl.md) — db.js SSL must be env-aware: false in dev, {rejectUnauthorized:false} in production; never unconditionally false.
- [Seed file security](seed-security.md) — seed.sql is gitignored; admin password stored as bcrypt hash not plaintext; login supports bcrypt+plaintext fallback in auth route.
- [PostgreSQL backend wiring](pg-backend-wiring.md) — full API wiring complete; staff_advance_requests uses request_date not date; /staff/advance-requests route must be registered BEFORE /:id or Express catches it as integer param.
- [Admin credentials management](admin-credentials.md) — PATCH /settings/admins/:id/credentials hashes with bcrypt(10); login supports bcrypt+plaintext fallback; api.settings.admins.patchCredentials().
- [PUT routes fetch-first pattern](put-fetch-first.md) — all PUT routes now fetch-first (SELECT then merge); external-debts POST accepts `dir` alias; GET /:id added for receipt/payment vouchers; surgery route is /surgery-inventory; auth login is /settings/auth/login; test_api.sh covers 63 endpoints, all auth-protected routes use check_auth.
- [Session payment drawer dedup](session-drawer-dedup.md) — sessions POST MUST NOT auto-insert drawer_transaction; frontend doDeposit() already calls api.drawers.transactions.create()+updateBalance() with correct balance_after. Duplicate removed; is_auto flag marks backend-only auto entries if ever re-added.
- [Finance reset-all stub](finance-reset-all.md) — POST /finance/reset-all is a dead stub (wrong table names); now returns 410 with requireAdmin guard. Use POST /api/admin/execute-delete (targetCategory: patients/financials/all) for actual bulk deletes.
- [App polling on 1s interval](app-polling.md) — DB LOAD useEffect sets a 1-second setInterval calling _doLoad() (skips when user is typing); logs show repeated [DB LOAD] entries by design, not a loop bug; {} in log capture is serialization artifact.
- [Voucher name/reason fields](voucher-name-fields.md) — patient/insurance/staff/supplier voucher names must be picked from a searchable dropdown (no free text); free text only allowed for "أخرى" type.
- [Production deployment topology](prod-deployment-topology.md) — production runs on external cPanel/Hostinger hosting with its own DB, not a Replit deployment; agent's DATABASE_URL only reaches the dev DB.
- [Generic table admin endpoint](admin-table-endpoint.md) — /api/admin/tables (GET list+counts, DELETE /:table) is allow-listed against every schema.sql table; used by DataDeletionScreen's per-table delete UI.
- [Session diagnoses/medications persistence](session-diag-meds-persistence.md) — sessions table has no diagnoses/medications columns; must persist via session_diagnoses/session_medications sub-tables or data silently vanishes on reload.
- [Backup export/restore on shared hosting](backup-export-hosting.md) — pg_dump/psql binaries unavailable on Hostinger; pure-JS dump needed; strict statement ordering; never test restores against live DB with search_path tricks.
- [Staff permissions unified (reversal)](subitem-permission-system.md) — second permission system was removed; only DEPT_PERM_TREE/DeptPermissions remains — extend it, don't rebuild a parallel system.
- [Patient file financial segregation](patient-file-financial-segregation.md) — PatientFileScreen gates invoice/paid/debt figures per dept via canSeeFinance(); medical history (diagnoses/meds/refs) stays unified.
- [Dual-layout global banners](dual-layout-banners.md) — App() (admin) and StaffPortal are separate JSX trees; global banners/props must be added to both independently, no shared shell.
- [Print report architecture](print-report-architecture.md) — all printing goes through printHtml/savePdfHtml via a standalone iframe doc; department "طباعة" buttons are frequently unwired or missing entry-data capture — check onClick and state persistence, not just CSS.
- [Permission tree scope discipline](permission-tree-scope.md) — when adding a new staff permission checkbox, only add it if a matching real UI action already exists; drop permissions with no corresponding feature instead of building new UI to justify them.
- [DB-vs-UI permission audits](db-ui-permission-audit.md) — when asked to sync staff_dept_permissions columns with the UI, diff psql `\d` column list against DeptPermissions type; wire only columns with a real gating feature, skip orphan DB columns like can_edit_voucher.
- [Queues results persistence](queues-results-persistence.md) — status-only API calls can still hide a connectivity bug if the actual entered content (results/reports) never reaches the backend; also watch for menu-linked duplicate screens still on mock data.
- [Staff vs admin screen reachability](staff-vs-admin-reachability.md) — StaffPortal has its own render switch; screens only in admin's renderScreen (financial/staff-mgmt/settings/admin-notes) are unreachable by staff — don't build permission checkboxes for them.
- [Financial charts static arrays bug](financial-charts-static.md) — revenueByDept/monthlyTrend/withdrawCats were module-level empty arrays; must be computed dynamically inside FinancialSummaryScreen/FinProfitScreen from filteredTxs.
- [Dashboard Palestine timezone](dashboard-tz.md) — getWeekStart/getMonthStart/7-day loop must use _jlmNow()+UTC methods, not new Date() local methods; tx_date from pg DATE column arrives as ISO timestamp string "YYYY-MM-DDT00:00:00.000Z", slice(0,10) correctly extracts date.
- [Data Deletion FK ordering](data-deletion-fk-ordering.md) — execute-delete and per-table delete must clear debts+patient_delete_requests BEFORE patients; PRE_DELETE_STEPS map handles per-table cascade.
- [App.tsx module split](app-module-split.md) — types.ts/constants.ts/utils.ts/toastStore.ts/components/shared.tsx extracted; print globals (gPrintSettings etc.) stay in App.tsx as `let` because they are reassigned and named imports are read-only.
- [Financial audit facts](financial-audit-facts.md) — staff API is /staff not /staff/members; PERM_COLS_ALLOWED drops ~50 fine-grained perms (keeps 8); daily/shift salary configured but net-pay never uses attendance; voucher DELETE routes unprotected.
- [Secure ledger](secure-ledger.md) — ledger_entries is append-only DB-enforced truth; balance=SUM(CREDIT-DEBIT); single choke-point POST /drawers/transactions computes balance server-side; vouchers record document only (forward-only double-count fix); requireFinancialAuth is identity-only by design.

- [Dept perm tree derived](dept-perm-tree-derived.md) — DEPT_PERM_TREE is generated from NAV_ITEMS_BASE (single source of truth) via buildDeptPermTree(); drawer subsection removed from tree but canDrawer* keys kept; dept PV personal-expense reason sets category.
- [Unified financial engine](unified-financial-engine.md) — calculateFinancials() is the only profit engine; FinancialSummaryScreen uses allPaymentVouchers prop (not paymentVouchers, which conflicts with local state); StaffPortal passes allPaymentVouchers; receiptVouchersGlobal+paymentVouchersGlobal loaded in _doLoad Promise.all.
